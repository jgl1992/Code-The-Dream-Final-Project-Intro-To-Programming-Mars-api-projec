/* html */
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="70 Windy" />
    <link rel="stylesheet" href="./index.css" />
    <title>70 Windy</title>
  </head>
  <body class="main-page">
    <section class="weather-screen">
      <div class="weather-content">
        <h1>70 Windy</h1>
        <p>
          Step into a windy heat! Enjoy your paradise by finding the temperature and windy condition. and indulge in paradises in Fahrenheit and Kelvin. 
          Explore your own weather condition, build your jacket gear, and explore in-depth bliss exploring.
        </p>
        <button class="start-button">
          <a href="./temp.html">Let's get sweaty!</a>
        </button>
      </div>
    </section>
  </body>
</html>

/* CSS */
html {
  font-size: 12px;
}
body {
  font-family: "Playwrite NZ", sans-serif;
  font-weight: 400;
}
body.main
  background-size: cover;
  background-position: center;
}
.temperature-content {
  display: flex;               
  gap: 5rem;
  align-content: center;
}
.temperature-content {
  font-family: "Playwrite New Zealand", sans-serif;
  font-weight: 600;          
}

.windy-page-layout {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  grid-template-rows: auto 1fr auto 1fr
  gap: 1rem;
}
.windy-page-layout .header-block {
  grid-column: 1 / -1;     
  grid-row: 1;
}

/*JS */
const tempFDisplay = document/getElementbyId('tempF');
const descDisplay = document.getElementyId('desc');
const conditionsView = document.getElementbyId('cond');

async function getTemp(url) {
  try {
    const response = await fetch(url);
    if (!response.ok) throw new Error(response.status);
    const data = await response.json();
    return data.results.map((item) => ({
      id: item.uid,
    }));
  } catch (error) {
    console.error("Error fetching temp", error);
    return null;
  }
}
console.log(TempData)
async function getTempInfo(hawaii) {
  try {
    const response = await fetch(`${tempUrl}${temp.id}`);
    if (!response.ok) throw new Error(response.status);
    const data = await response.json();
    return data.result.properties;
  } catch (error) {
    console.error("Error fetching player info:", error);
    return null;
  }
}

const url = 'https://api.open-meteo.com/v1/forecast';
const responses = await fetchWeatherApi(url, params);

// Process the first location
const response = responses[0];
const current = response.current();
console.log(`Current temperature: ${current.variables(0).value()}°C`);
```

async function getBoxWeather() {
    const city = cityInput.value.trim();
    if (!city) return;
    errorMsg.textContent = "";
    resultDiv.className = "result-hidden";

    try {
        // 1. Get ready for conditioner 
const weatherUrl = `https://api.open-meteo.com/v1/forecast?latitude=${latitude}&longitude=${longitude}&current=temperature_2m,weather_code`;
const weatherRes = await fetch(weatherUrl);
const weatherData = await weatherRes.json();

//2. Get ready for temp const celsius = weatherData.current.temperature_2m;
Async function getWeather(latitude, longitude)
{
Try
{
/*build */
Const url = `https://api.open-meteo.com/v1/forecast?latitude=${latitude}&longitude=${longitude}&current_weather=true&hourly=temperature_2m`;
/*fetch */
Const response = await fetch(url);
/*check 4 errorz */
If(!response.ok {
throw new ERROR (‘http error! Status: ${response.status});
}
/* parse Jason*/
Const data = await response.json();
/* result*/
Console.log(“current weather:”);
Console.log(‘temperature: ${data.current_weather.temperaturre});
Console.log(‘wind speed: ${data.current_weather.windspeed} km/h);
return data;
} catch (error) {
console.error(“error fetching weather”, error.message);
getWeather( 41.314159, -89.6796







      
}
