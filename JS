/* html */
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Paradise " />
    <link rel="stylesheet" href="./index.css" />
    <title>Marvel Paradise Players</title>
  </head>
  <body class="main-page">
    <section class="player-screen">
      <div class="player-content">
        <h1>Marvel Paradise Players</h1>
        <p>
          Step into a paradise! Enjoy your paradise by finding the temperature and sunny condition. and indulge in paradises 1610B and 6160. 
          Explore your own fantasy, build your family, and explore in-depth bliss.
        </p>
        <button class="start-button">
          <a href="./match.html">Find your temp!</a>
        </button>
      </div>
    </section>
  </body>
</html>


/* CSS */
html {
  font-size: 12px;
}
body {
  font-family: "Playwrite NZ", sans-serif;
  font-weight: 400;
}
body.main
  background-image: url("images/reality/2.png");
  background-size: cover;
  background-position: center;
}
.temperature-content {
  display: flex;               
  gap: 5rem;
  align-content: center;
}
.temperature-content {
  font-family: "Playwrite New Zealand", sans-serif;
  font-weight: 600;          
}

.craft-page-layout {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  grid-template-rows: auto 1fr auto 1fr
  gap: 1rem;
}
.craft-page-layout .header-block {
  grid-column: 1 / -1;     
  grid-row: 1;
}



/*JS */
const tempUrl = "'https://api.open-meteo.com/v1/forecast'

async function getTemp(url) {
  try {
    const response = await fetch(url);
    if (!response.ok) throw new Error(response.status);
    const data = await response.json();
    return data.results.map((item) => ({
      id: item.uid,
    }));
  } catch (error) {
    console.error("Error fetching temp", error);
    return null;
  }
}
console.log(TempData)
async function getTempInfo(hawaii) {
  try {
    const response = await fetch(`${tempUrl}${temp.id}`);
    if (!response.ok) throw new Error(response.status);
    const data = await response.json();
    return data.result.properties;
  } catch (error) {
    console.error("Error fetching player info:", error);
    return null;
  }
}
  let chooseParadise = document.querySelector(".paradise");
  let paradiseCard = document.createElement("img");
  console.log(`The paradise is: ${gameState.paradise}`);

mport { fetchWeatherApi } from 'openmeteo';

const params = {
  latitude: 52.52,
  longitude: 13.41,
  current: 'temperature_2m,weather_code',
  hourly: 'temperature_2m',
  daily: 'weather_code,temperature_2m_max,temperature_2m_min'
};
const url = 'https://api.open-meteo.com/v1/forecast';
const responses = await fetchWeatherApi(url, params);

// Process the first location
const response = responses[0];
const current = response.current();
console.log(`Current temperature: ${current.variables(0).value()}°C`);
```

/*
